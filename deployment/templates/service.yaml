apiVersion: v1
kind: Service
metadata:
  name: gmr-linguistics
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: gmr-linguistics
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

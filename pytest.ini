[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
markers =
    integration: tests that require external services (pg, neo4j, mistral-stub)
